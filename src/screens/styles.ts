import {StyleSheet} from 'react-native'

export const SEARCH_BAR_HEIGHT = 102

const styles = StyleSheet.create({
  container: {
    flex: 1
  },
  stack: {
    ...StyleSheet.absoluteFillObject
  },
  slidingPanel: {
    backgroundColor: '#FFF'
  }
})

export default styles
